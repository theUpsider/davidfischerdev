name: Build and Deploy

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests (if any)
        run: npm test --if-present

      - name: Build for production
        run: npm run build-prod
        env:
          VITE_API_BASE_URL: https://davidfischer.dev:5000

      - name: Build Docker image
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
        run: |
          docker build -t davidfischerdev-frontend \
            --build-arg VITE_API_BASE_URL=https://davidfischer.dev:5000 \
            --build-arg NODE_ENV=production .

    # Add your deployment steps here
    # Example for Docker registry:
    # - name: Push to Docker registry
    #   if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    #   run: |
    #     echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
    #     docker tag davidfischerdev-frontend your-registry/davidfischerdev-frontend:latest
    #     docker push your-registry/davidfischerdev-frontend:latest
